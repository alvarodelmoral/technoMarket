{% extends "base.html.twig" %}
{% block titulo %}Productos - {{ parent() }}{% endblock %}
{% block sidebar %}
            {{ render(path('menu', {seccionActual : "productos"})) }} 
{% endblock %}
{% block body %}
<div class = "container">
    <h1>Insertar nuevo producto</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
</div>
{% endblock %}
